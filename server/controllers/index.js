import * as inventoryController from './inventoryController';
import * as commandController from './commandLineController';

let controllers = {
	inventory:inventoryController,
	command:commandController
};

export default controllers;